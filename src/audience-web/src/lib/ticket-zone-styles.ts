import type { TicketType } from "./types";

export const ticketZoneColors: Record<TicketType["zone"], string> = {
  svip: "#7c3aed",
  vip: "#2563eb",
  cat1: "#00b159",
  cat2: "#d97706",
  ga: "#64748b",
};
