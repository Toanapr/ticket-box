export enum PaymentStatus {
  CREATED = 'created',
  PENDING = 'pending',
  PENDING_RECONCILIATION = 'pending_reconciliation',
  SUCCEEDED = 'succeeded',
  FAILED = 'failed',
  EXPIRED = 'expired',
}
